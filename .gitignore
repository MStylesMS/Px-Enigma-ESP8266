.pio/
.pioenvs/
.piolibdeps/
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch/
.DS_Store
Thumbs.db

# Legacy reference implementation — kept in-tree for hardware reference only,
# not part of the active codebase.
archive/

# Local secrets / runtime config — commit config.json.example instead and copy
# it to data/config.json before running `pio run -t uploadfs`.
src/secrets.h
data/config.json
data/secrets.json

# Tooling artifacts
tools/**/node_modules/
